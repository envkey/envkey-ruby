require "spec_helper"

describe EnvkeySdk do
  it "has a version number" do
    expect(EnvkeySdk::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
